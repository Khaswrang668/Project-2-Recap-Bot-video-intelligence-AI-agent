# Recap-Bot
A full stack web-application with an AI tool that enables clients and users to upload videos to the platform ask questions regarding the video.The video is processed and stored as embeddings in supabase pgvector db which the AI fetches semantic search to answer user's queries more effectively 
